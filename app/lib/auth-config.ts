// Central auth feature flag.
//
// Set this to `true` to re-enable OAuth across the app.
// The page components and proxy already contain the required logic and will
// start using the existing Auth.js setup again without further code changes.
export const AUTH_ENABLED = false;